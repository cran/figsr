% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figs_tree.R
\name{fit_figs}
\alias{fit_figs}
\title{Fitting Bridge for the Parsnip Interface}
\usage{
fit_figs(
  formula = NULL,
  data = NULL,
  x = NULL,
  y = NULL,
  max_splits = 10,
  max_trees = NULL,
  min_n = 5,
  mode = "regression",
  ...
)
}
\arguments{
\item{formula}{A formula specifying outcome and predictors.}

\item{data}{A data frame containing the training data.}

\item{x}{A data frame or matrix of predictors.}

\item{y}{An outcome vector.}

\item{max_splits}{Integer. Maximum total number of splits across all trees.}

\item{max_trees}{Integer. Maximum number of trees in the sum.}

\item{min_n}{Integer. Minimum number of observations in a node to split.}

\item{mode}{Character. Either `"regression"` or `"classification"`;
classification supports two-class outcomes only.}

\item{...}{Additional arguments passed to [figs()].}
}
\value{
An object of class `figsr_fit`.
}
\description{
`fit_figs()` is the function `parsnip` calls when fitting a [figs_tree()]
specification with the `"figsr"` engine. It accepts either the formula
interface or the `x`/`y` interface and forwards to [figs()]. Users normally
call [figs()] or `fit()` instead.
}
\examples{
set.seed(42)
df <- data.frame(x1 = rnorm(60), x2 = rnorm(60))
df$y <- 2 * (df$x1 > 0) + rnorm(60, sd = 0.2)
fit_figs(x = df[, c("x1", "x2")], y = df$y, max_splits = 3)
}
